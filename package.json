{
  "name": "ritm-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-social-card.cjs && node build-static.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "sharp": "^0.35.4"
  }
}
